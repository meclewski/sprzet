{% extends 'base.html.twig' %}

{% block body %}
    <h1>Places list</h1>

    <table class="table table-striped table-bordered" id="table" width="100%" cellspacing="0">
        <thead>
            <tr>
                <th>Id</th>
                <th>Plname</th>
                <th>Pldesc</th>
                <th class="hidden-xs">Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for place in places %}
            <tr>
                <td><a href="{{ path('place_show', { 'id': place.id }) }}">{{ place.id }}</a></td>
                <td>{{ place.plName }}</td>
                <td>{{ place.plDesc }}</td>
                <td class="hidden-xs">
                    <div class="btn-group pull-right">
                            <a href="{{ path('place_show', { 'id': place.id }) }}" class="btn btn-sm btn-primary" role="button">show</a>
                        
                            <a href="{{ path('place_edit', { 'id': place.id }) }}" class="btn btn-sm btn-primary" role="button">edit</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('place_new') }}">Create a new place</a>
        </li>
    </ul>
{% endblock %}
