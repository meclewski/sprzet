{% extends 'base.html.twig' %}

{% block body %}
    <h1>Rodzaj urządzenia</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Twórz" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('efunction_index') }}">Powrót do listy</a>
        </li>
    </ul>
{% endblock %}
